{
  "copilot_action_name": "LINUX-LIST-USERS-COPILOT-ACTION",
  "description": "Enumerates local Linux users via getent, skipping common service accounts. For each user, collects username, full name (GECOS), home directory, group memberships, last login (from lastlog), and password status (required/expired from /etc/shadow). Outputs a single NDJSON object and writes atomically to the Active Response log with .new fallback.",
  "technology": "Linux",
  "icon": "linux",
  "script_parameters": [
    {
      "name": "ARLog",
      "type": "string",
      "required": false,
      "description": "Active Response NDJSON output path (atomic overwrite; falls back to .new if locked).",
      "default": "/var/ossec/active-response/active-responses.log"
    },
    {
      "name": "LogPath",
      "type": "string",
      "required": false,
      "description": "Path to the rotating human-readable script log.",
      "default": "/tmp/ListLinuxUsers-script.log"
    },
    {
      "name": "LogMaxKB",
      "type": "integer",
      "required": false,
      "description": "Maximum size of the log file (KB) before rotation.",
      "default": 100
    },
    {
      "name": "LogKeep",
      "type": "integer",
      "required": false,
      "description": "Number of rotated log files to keep.",
      "default": 5
    }
  ],
  "repo_url": "https://raw.githubusercontent.com/socfortress/LINUX-LIST-USERS-COPILOT-ACTION/refs/heads/main",
  "script_name": "List-linux-users.sh",
  "version": "1.0.0",
  "last_updated": "2025-08-23T00:00:00Z"
}
